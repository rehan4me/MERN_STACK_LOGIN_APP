# React.js & Node.js with Passport Login

This is a user login and registration app using React.js, Node.js, Express, Passport, Mongoose and some other packages.


### Usage

```sh
$ docker-compose up --build
```

```sh
# open http://localhost:3000
```
